import React from 'react';

const ForwardedInboxPage: React.FC = () => {
  // Placeholder content until real data integration
  return (
    <div className="min-h-screen bg-gray-50 p-6">
      <h2 className="text-2xl font-semibold mb-4">Forwarded Applications</h2>
      <p className="text-gray-600">This is the forwarded inbox page. Implement table/list of forwarded applications here.</p>
    </div>
  );
};

export default ForwardedInboxPage;
