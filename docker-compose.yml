services:

  backend:
    build:
      context: ./backend
      dockerfile: Dockerfile.prod
    container_name: alms-backend
    env_file:
      - ./backend/.env
      - ./.env
    environment:
      DATABASE_URL: ${DATABASE_URL}   # must point to AWS RDS
      NODE_ENV: production
    ports:
      - "3000:3000"
    networks:
      - alms-net

  frontend:
    build:
      context: ./frontend
      dockerfile: Dockerfile.prod
      args:
        NEXT_PUBLIC_API_URL: ${NEXT_PUBLIC_API_URL}
    container_name: alms-frontend
    env_file:
      - ./.env
    environment:
      NEXT_PUBLIC_API_URL: ${NEXT_PUBLIC_API_URL}
      NODE_ENV: production
    depends_on:
      - backend
    networks:
      - alms-net

  nginx:
    image: nginx:stable-alpine
    container_name: alms-nginx
    ports:
      - "80:80"
      - "443:443"
    volumes:
      - ./nginx/nginx.conf:/etc/nginx/nginx.conf:ro
      - /etc/letsencrypt/live/dev.alms.sparquer.com/fullchain.pem:/etc/ssl/certs/fullchain.pem:ro
      - /etc/letsencrypt/live/dev.alms.sparquer.com/privkey.pem:/etc/ssl/private/privkey.pem:ro
    depends_on:
      - frontend
      - backend
    networks:
      - alms-net

networks:
  alms-net:
    driver: bridge
